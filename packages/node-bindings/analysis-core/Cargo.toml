[package]
name = "analysis-core-node-binding"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "OpenAlice analysis_core Node-API binding crate (bootstrap shell, no DTO bridge yet)."

[lib]
crate-type = ["rlib"]

[dependencies]
analysis-core = { path = "../../../crates/analysis-core" }
